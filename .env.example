# Copy to `.env` for local development.
#
# Dev (SQLite):
DATABASE_URL="file:./dev.db"
#
# Prod (Postgres) — also set `provider = "postgresql"` in prisma/schema.prisma:
# DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/strategic_line?schema=public"
